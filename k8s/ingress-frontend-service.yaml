apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: ingress-frontend-service
spec:
  rules:
  - http:
      paths:
      - path: /
        backend:
          serviceName: frontend
          servicePort: 80
